import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles({
  header: {
    position: "fixed",
    width: "100%",
    margin: 0,
    padding: "0 !important",
  
  },
  body: {
    marginTop: 62,
  },
});
